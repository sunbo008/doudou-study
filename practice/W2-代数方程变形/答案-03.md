# W2 答案 03 · 限时综合复测

## 一

1. `a−5`；`5`
2. `x`；`0`
3. `0`；`1`
4. **是**

## 二

5. `0` 或 `1`
6. `5` 或 `6`
7. `2` 或 `3`
8. `−1` 或 `0`
9. `0` 或 `1/2`
10. `0` 或 `1`
11. `4` 或 `5`
12. `0` 或 `1/3`

## 三

13. 要点任选其一即可：  
    - 会丢掉 `a−5=0`（即 `a=5`）这个解；  
    - 除数不能为零，但 `a=5` 代入原式仍成立。
14. `② → ④ → ③ → ①`

## 达标判定

- ≥ 12 / 14，且第 13 题正确：可记「独立同类」候选，3 天后做变式复测。
- 第 13 题错：无论总分多少，先回讲义坑 1 + 卷 02 的 C 组。
- ≤ 10 题：回卷 01（约分）与卷 02（正路填空）。
